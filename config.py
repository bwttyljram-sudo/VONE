"""
إعدادات البوت — كل القيم الحساسة تُقرأ من متغيرات البيئة (Environment Variables)
حتى لا يتم رفع التوكن أو أي بيانات حساسة داخل الكود على GitHub مثلاً.

عند النشر على Render، أضف هذه المتغيرات من لوحة تحكم الخدمة (Environment):
    BOT_TOKEN        -> توكن البوت من BotFather
    ADMIN_ID          -> آيدي المطور/الأدمن (رقم)
    CHANNEL_USERNAME  -> يوزر القناة بصيغة @اسم_القناة (يجب أن يكون البوت أدمن فيها)
    CHANNEL_LINK      -> رابط القناة الكامل (يُستخدم في زر الاشتراك)
    PORT              -> يوفرها Render تلقائياً، لا حاجة لتعديلها يدوياً غالباً
"""

import os

BOT_TOKEN = os.environ.get("BOT_TOKEN", "").strip()

ADMIN_ID = int(os.environ.get("ADMIN_ID", "6043858925"))

# يوزر القناة يُستخدم في استدعاء get_chat_member للتحقق من الاشتراك.
# لازم يكون بصيغة @username وأن يكون البوت أدمن داخل القناة.
CHANNEL_USERNAME = os.environ.get("CHANNEL_USERNAME", "@ZenoX_Tools").strip()

# رابط القناة الظاهر في زر "اشترك في القناة"
CHANNEL_LINK = os.environ.get("CHANNEL_LINK", "https://t.me/ZenoX_Tools").strip()

# منفذ السيرفر الوهمي (Render يوفره تلقائياً عبر متغير PORT)
PORT = int(os.environ.get("PORT", "10000"))

# مسار قاعدة البيانات (ملف SQLite محلي، يكفي لهذا الحجم من الاستخدام)
DB_PATH = os.environ.get("DB_PATH", "bot_database.db")

# ------------------ إعدادات الحماية والأداء ------------------

# أقصى عدد عمليات تحويل نص→صوت تُنفَّذ في نفس اللحظة على مستوى البوت كله
# (يحمي المعالج الضعيف لاستضافة Render المجانية من الانهيار تحت الضغط)
MAX_CONCURRENT_TTS = 5

# أقل مدة (بالثواني) يجب أن يمر بين طلبين من نفس المستخدم
# يمنع السبام الفردي ويحمي البوت من التعليق
PER_USER_COOLDOWN_SECONDS = 3

# أقصى عدد أحرف مسموح تحويلها في الطلب الواحد (حماية للمعالج والذاكرة)
MAX_TEXT_LENGTH = 2000

# التأخير بين كل رسالة وأخرى أثناء الإذاعة الجماعية (بالثواني)
# قيمة منخفضة كفاية لإرسال سريع لكنها لا تصطدم بحدود تيليجرام للحظر
BROADCAST_DELAY_SECONDS = 0.05

# عدد الأصوات المعروضة في كل صفحة من القائمة
VOICES_PER_PAGE = 10

# اعتبار المستخدم "نشط الآن" إذا كان آخر تفاعل له خلال هذه المدة (بالدقائق)
ACTIVE_NOW_WINDOW_MINUTES = 5
